name: schedule_ui
description: Teaching Schedule Manage project
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # HTTP requests
  http: ^1.5.0

  # Local storage
  shared_preferences: ^2.5.3

  # State management
  provider: ^6.1.1

  # Date formatting
  intl: ^0.19.0

  # Icons
  font_awesome_flutter: ^10.6.0

  # Charts
  fl_chart: ^0.68.0

  # Data tables
  data_table_2: ^2.5.6

  # Loading indicators
  shimmer: ^3.0.0

  # Routing
  go_router: ^14.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
